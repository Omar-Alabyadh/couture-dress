.vercel
.env*.local
node_modules
.next
out
coverage
dist
*.tsbuildinfo

# AR: نُبقي الصور المصدرية في `assets/` فقط؛ Next ينسخها إلى `public/assets` عبر السكربت.
# EN: Source images live in `assets/`; Next copies to `public/assets` via the sync script.
public/assets/*
!public/assets/.gitkeep
