.env
.vercel
# Cursor — تتبّع قواعد المشروع فقط، تجاهل باقي إعدادات IDE المحلية
.cursor/*
!.cursor/rules/
.env*.local
node_modules
src/generated/prisma
.next
out
coverage
dist
*.tsbuildinfo
# Auto-generated by Next.js on every dev/build — should not be tracked
next-env.d.ts

# AR: نُبقي الصور المصدرية في `assets/` فقط؛ Next ينسخها إلى `public/assets` عبر السكربت.
# EN: Source images live in `assets/`; Next copies to `public/assets` via the sync script.
public/assets/*
!public/assets/.gitkeep
